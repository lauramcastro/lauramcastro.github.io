# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.
#
# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.
title: "Laura M. Castro"
author: "Laura M. Castro"
email: lcastro@udc.gal
description: >- # this means to ignore newlines until "baseurl:"
  Professor at Universidade da Coruña (UDC), 
  director of CEXEF (Gender and Feminist Studies Centre).
  member of the BEAM and the Erlang Ecosystem Foundation.
baseurl: "" # the subpath of your site, e.g. /blog
url: https://lauramcastro.github.io # the base hostname & protocol for your site, e.g. http://example.com
## social links
#github_username:  lauramcastro
#rss: RSS
#linkedin_username: lauracastro
show_excerpts: true
# Build settings
theme: minima
plugins:
  - jekyll-feed
minima:
  skin: auto
# Archives
collections:
  archives:
    output: true
    permalink: /archives/:path/
# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal "default list".
#
# Excluded items can be processed by explicitly listing the directories or
# their entries' file path in the `include:` list.
#
exclude:
#   - .sass-cache/
#   - .jekyll-cache/
#   - gemfiles/
   - Gemfile
   - Gemfile.lock
#   - node_modules/
#   - vendor/bundle/
#   - vendor/cache/
#   - vendor/gems/
#   - vendor/ruby/
